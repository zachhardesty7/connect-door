{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"command": "cd ${cwd}; yarn dev",
			"label": "yarn dev",
			"detail": "link to dev library, run watcher & gatsby",
			"group": {
				"kind": "test",
				"isDefault": true
			}
		}
	]
}
